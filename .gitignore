# python
.venv/
__pycache__/
*.pyc

# Test data (large zips, not for version control)
testing/

# Claude Code memory (personal/local)
.claude/

# Local OCI provisioning script — contains personal tenancy/subnet OCIDs and
# local machine paths. Kept on disk to run locally, never committed.
deploy/grab-a1.ps1
